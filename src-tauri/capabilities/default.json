{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Lumina desktop window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}